"""Linting rules for the codebase."""

UNIX = """
Use `./.venv/bin/pylint` to address linting errors throughout this codebase. stick to these rules: line too long, trailing newlines, missing module docstring, missing function or method docstring, unused imports, trailing whitespace, fstring that does not interpolate. keep the docstrings concise.
"""

WIN10 = """
Use `.\.venv\Scripts\pylint` to address linting errors throughout this codebase. stick to these rules: line too long, trailing newlines, missing module docstring, missing function or method docstring, unused imports, trailing whitespace, fstring that does not interpolate. keep the docstrings concise.
"""
